export function IssueDetailsPage() {
  // TODO:
  // Eventually this page will:
  //
  // - read :id using useParams()
  // - inspect navigation state using useLocation()
  // - fall back to getIssue(id)
  // - show loading state
  // - show "not found" state
  // - render Issue data with shadcn components
  //
  // For now just return:
  // <h1>Issue Details</h1>

  return null;
}
